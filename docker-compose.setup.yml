services:
  # postgres:
  #   image: postgres:17-alpine
  #   container_name: mail-kit-postgres
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: root
  #     POSTGRES_PASSWORD: admin
  #     POSTGRES_DB: mailkit
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U root -d mailkit"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  # redis:
  #   image: redis:7-alpine
  #   container_name: mail-kit-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   command: ["redis-server", "--appendonly", "yes"]
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  mailpit:
    image: axllent/mailpit:latest
    container_name: mail-kit-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"

# volumes:
#   postgres_data:
#   redis_data:
